# torch installed separately — do NOT put torch here
# Triton ships inside the torch wheel on Linux; floor only, don't downgrade it.
triton>=3.2.0

# Qwen3 support (and its tokenizer format) needs a 2025-era transformers;
# 4.44.2 fails with "did not match any variant of untagged enum ModelWrapper".
transformers>=4.51
accelerate>=0.33.0
bitsandbytes>=0.43.3
tokenizers>=0.21
sentencepiece
einops

ninja
pybind11>=2.12
packaging

numpy==1.26.4
scipy
pandas
matplotlib
seaborn
tqdm
wandb

pytest
pytest-benchmark